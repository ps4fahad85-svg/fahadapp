from django.urls import path

app_name = "consultations"

urlpatterns = [
    # سيتم إضافة مسارات الاستشارات لاحقاً
    # مثال مستقبلي:
    # path("book/", views.book_session, name="book_session"),
]
